import {expect} from 'chai';
import {Kernel} from '../src/kernel';

describe("KERNEL", ()=>{
  it("can bind interface to class", ()=>{

    let kernel = new Kernel();

    expect(true).to.be.true;
  })
})
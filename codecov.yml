coverage:
  status:
    project:
      default:
        threshold: 0.10%
      pigly:
        flags: pigly
      transformer:
        flags: transformer
    patch: off
flags:
  pigly:
    paths:
      - packages/pigly/src
  transformer:
    paths:
      - packages/pigly-transformer/src

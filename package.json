{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.2"
  },
  "workspaces": {
    "packages": ["packages/*"]
  },
  "version": "0.0.0",
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "coverage": "lerna run coverage"
  }
}
